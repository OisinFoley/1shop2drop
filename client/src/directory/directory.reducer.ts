import { sections, DirectoryState } from '.';
const INITIAL_STATE = { sections };

const directoryReducer = (state: DirectoryState = INITIAL_STATE) => {
  return state;
};

export { directoryReducer };
